// Simple form validation example (can be extended further)
const form = document.querySelector('.contact-info form');

form.addEventListener('submit', (event) => {
  const name = document.getElementById('
